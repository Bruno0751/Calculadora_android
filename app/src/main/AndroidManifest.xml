<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.view">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_caculadora"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calculadora">
        <activity
            android:name=".ActivitySobre"
            android:label="@string/title_activity_sobre"
            android:theme="@style/Theme.Calculadora.NoActionBar"></activity>
        <activity
            android:name=".ActivityMain"
            android:label="@string/app_name"
            android:theme="@style/Theme.Calculadora.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>